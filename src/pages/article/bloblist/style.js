import styled from "styled-components";
export const BlobListWrapper = styled.div`
  .article_content {
    width: 300px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
`;
